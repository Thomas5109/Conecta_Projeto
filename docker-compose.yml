services:
  postgres:
    image: postgres:18
    container_name: projeto-conecta-postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: projeto_conecta
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: